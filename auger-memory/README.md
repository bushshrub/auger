LangGraph’s BaseStore Idea

a key-value store with hierarchical namespaces, where different memory categories live under different prefixes rather than in different tables.

Feature
light weight for single user, directly writes to disk
 